import Button from '@/shared/ui/Button';
import styles from './index.module.scss';

function CrewInviteButton() {
  return <Button className={styles.inviteButton}>+ 초대</Button>;
}

export default CrewInviteButton;
